# Entropy - dependencies
#
# Kept deliberately minimal. Each stage adds only what it needs, so that if an
# install breaks you know exactly which change caused it. Streamlit Community
# Cloud installs this file verbatim, so every line here costs deploy time and
# memory.

# --- Stage 1: corpus handling ---------------------------------------------
pandas>=2.0,<3.0
python-dateutil>=2.9

# --- Stages 2-4: parsing, NER, relations, events --------------------------
spacy>=3.8,<3.9
en_core_web_sm @ https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.8.0/en_core_web_sm-3.8.0-py3-none-any.whl

# --- Stage 5: web interface -----------------------------------------------
streamlit>=1.49

# --- Planned but not needed -----------------------------------------------
# dateparser   Stage 5 TIMEX normalisation is hand-rolled on datetime + re.
# networkx     entropy/graph.py uses plain dicts and an adjacency index.
# matplotlib   Streamlit's built-in charts cover every figure in the app.
# nltk         not used by the final pipeline.

# --- Optional: Hugging Face comparison baseline ---------------------------
# DO NOT uncomment for Streamlit Community Cloud - torch is far larger than
# the ~1 GB deploy budget, and the app never loads the model. Install locally:
#   pip install "transformers>=4.40"
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
# transformers>=4.40
# torch